# MiniSwap 수정 작업 제안서 (2026-03-03)

> 기반 문서: `코드_UI_점검보고서_2026-03-02.md`
> 검증 방법: 소스코드 직접 대조 (컨트랙트, 테스트, 훅, 컴포넌트, 설정 파일)

---

## 검증 결과 총괄

| 이슈 | 보고서 내용 | 검증 결과 | 비고 |
|------|------------|----------|------|
| P0-1 테스트 실패 | constructor 인자 불일치 | **확인됨** | 5개 vs 4개 |
| P0-2 ESLint 실패 | 설정 파일 누락 | **확인됨** | config + 플러그인 모두 없음 |
| P0-3 네트워크 에러 | error 상태 미표출 | **확인됨** | App.jsx에서 error 미수신 |
| P1-1 번들 크기 | 코드 스플리팅 없음 | **확인됨** | vite.config.js에 build 옵션 없음 |
| P1-2 XSS 위험 | dangerouslySetInnerHTML | **확인됨** | stepper.jsx:47 |
| P1-3 탐색기 링크 | address 링크 사용 | **확인됨** | tx hash 미노출로 인한 의도적 선택 |
| P2-1 인코딩 | UTF-8 미명시 | 미검증 | 낮은 우선순위 |
| P2-2 목업 데이터 | mockData.js 기반 | **확인됨** | 온체인 연동 미구현 |

---

## P0-1. 컨트랙트 테스트 복구

### 문제 상세
- **컨트랙트**: `contracts/MiniSwapEscrow.sol:252` — constructor 인자 5개
  ```
  _usdt, _feeRecipient, _admin1, _admin2, _relayer
  ```
- **테스트**: `test/MiniSwapEscrow.test.js:91-106` — 배포 인자 4개 (`_relayer` 누락)
- **배포 스크립트**: `scripts/deploy.js:86-92` — 인자 5개 (정상)

### 수정 작업

#### 작업 1-1: deployFixture에 relayer 추가
```
파일: test/MiniSwapEscrow.test.js
위치: deployFixture 함수 (~91행)
```
- signers 배열에서 relayer 추출 추가
- `Escrow.deploy(...)` 호출에 5번째 인자 `relayer.address` 추가

#### 작업 1-2: relayer 관련 테스트 케이스 추가
- ZeroAddress 검증 테스트에 `_relayer = address(0)` 케이스 추가
- relayer 권한 테스트가 있다면 정합성 확인

#### 작업 1-3: 회귀 검증
```bash
npx hardhat test
```
- 102건 전체 통과 확인

### 예상 소요: 수정 30분 + 검증 15분

---

## P0-2. ESLint 설정 정상화

### 문제 상세
- `eslint@^9.0.0` 설치됨 (실제 9.39.3)
- 설정 파일 **전무** (eslint.config.js, .eslintrc.* 모두 없음)
- React 플러그인 미설치 (`eslint-plugin-react`, `eslint-plugin-react-hooks` 없음)

### 수정 작업

#### 작업 2-1: 필요 패키지 설치
```bash
npm install -D @eslint/js eslint-plugin-react eslint-plugin-react-hooks globals
```

#### 작업 2-2: eslint.config.js 생성 (Flat Config)
```
파일: eslint.config.js (신규 생성)
```
```js
import js from '@eslint/js'
import react from 'eslint-plugin-react'
import reactHooks from 'eslint-plugin-react-hooks'
import globals from 'globals'

export default [
  js.configs.recommended,
  {
    files: ['src/**/*.{js,jsx}'],
    plugins: { react, 'react-hooks': reactHooks },
    languageOptions: {
      ecmaVersion: 'latest',
      sourceType: 'module',
      globals: { ...globals.browser, ...globals.es2021 },
      parserOptions: { ecmaFeatures: { jsx: true } },
    },
    rules: {
      'react/react-in-jsx-scope': 'off',
      'react-hooks/rules-of-hooks': 'error',
      'react-hooks/exhaustive-deps': 'warn',
      'no-unused-vars': ['warn', { argsIgnorePattern: '^_' }],
    },
  },
  { ignores: ['dist/', 'node_modules/', 'artifacts/', 'cache/'] },
]
```

#### 작업 2-3: lint 스크립트 업데이트
```json
"lint": "eslint src"
```
> ESLint 9에서는 `--ext` 플래그가 deprecated. Flat Config의 `files` 패턴으로 대체.

#### 작업 2-4: 린트 실행 및 기존 에러 정리
```bash
npm run lint
```
- 기존 코드의 린트 에러 수량 파악 후 단계적 수정

### 예상 소요: 설정 20분 + 린트 에러 정리 1~2시간 (에러 수에 따라)

---

## P0-3. 네트워크 전환 실패 UI 피드백

### 문제 상세
- `useNetworkSwitch()` → `{ switchNetwork, switching, error }` 반환
- `App.jsx`에서 `error`를 **디스트럭처링하지 않음** (버려짐)
- `AppShell`에 error prop 미전달 → 헤더 경고배너에 실패 메시지 표시 불가
- `NetworkGuide.jsx`에서는 error를 정상 표시 (일관성 부족)

### 수정 방안 비교

| 방안 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **A. Toast 직접 호출** | useNetworkSwitch 내에서 toast 호출 | 간단, 한 곳 수정 | 훅이 UI 컨텍스트에 의존 |
| **B. App에서 error 전파** | App → AppShell → 배너에 표시 | 관심사 분리 | 3개 파일 수정 필요 |
| **C. useEffect + Toast** | App에서 error 감지 → toast 호출 | 기존 패턴과 일관 | 2개 파일 수정 |

### 권장: 방안 C (useEffect + Toast)

#### 작업 3-1: App.jsx에서 error 수신
```
파일: src/App.jsx
```
```js
// 변경 전
const { switchNetwork, switching: networkSwitching } = useNetworkSwitch()

// 변경 후
const { switchNetwork, switching: networkSwitching, error: networkError } = useNetworkSwitch()
```

#### 작업 3-2: useEffect로 Toast 연동
```
파일: src/App.jsx
```
```js
useEffect(() => {
  if (networkError) {
    toast(networkError, 'error')
  }
}, [networkError])
```

#### 작업 3-3: (선택) 경고 배너에 에러 메시지 인라인 표시
```
파일: src/components/AppShell.jsx
```
- `networkError` prop 추가
- 배너 하단에 에러 텍스트 + 재시도 버튼 표시

### 예상 소요: 30분

---

## P1-1. 번들 크기 최적화 (코드 스플리팅)

### 문제 상세
- `vite.config.js`에 `build` 옵션 전무
- 메인 청크 ~1MB+, WASM ~11MB (빌드 경고)

### 수정 작업

#### 작업 4-1: 라우트 기반 lazy import
```
파일: src/App.jsx
```
```js
const TradeRoom = lazy(() => import('./components/TradeRoom'))
const TradeHistory = lazy(() => import('./components/TradeHistory'))
const OrderDetail = lazy(() => import('./components/OrderDetail'))
```

#### 작업 4-2: vite.config.js에 빌드 최적화 추가
```
파일: vite.config.js
```
```js
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        vendor: ['react', 'react-dom'],
        wagmi: ['wagmi', 'viem', '@wagmi/core'],
        xmtp: ['@xmtp/browser-sdk'],
      },
    },
  },
  chunkSizeWarningLimit: 1000,
},
```

#### 작업 4-3: Suspense fallback 추가
- lazy import 컴포넌트를 `<Suspense fallback={<Loading />}>` 래핑

### 예상 소요: 1시간

---

## P1-2. dangerouslySetInnerHTML 제거

### 문제 상세
- `src/components/ui/stepper.jsx:47` — `dangerouslySetInnerHTML={{ __html: label }}`
- 현재는 내부 상수 기반이라 즉시 위험은 없으나, 확장 시 XSS 경로 가능

### 수정 작업

#### 작업 5-1: label을 plain text로 전환
```
파일: src/components/ui/stepper.jsx
```
```jsx
// 변경 전
<span dangerouslySetInnerHTML={{ __html: label }} />

// 변경 후 — 줄바꿈이 필요한 경우
<span>{label.split('<br>').map((line, i) => (
  <Fragment key={i}>{i > 0 && <br />}{line}</Fragment>
))}</span>
```

#### 작업 5-2: Stepper 호출부에서 HTML 문자열 제거
- label에 `<br>` 등 HTML 태그 사용하는 곳을 `\n` 또는 배열로 변환

### 예상 소요: 20분

---

## P1-3. 거래 완료 탐색기 링크 개선

### 문제 상세
- `src/components/TradeRoom.jsx:492` — `type: 'address'`로 사용자 주소 링크
- TX 해시가 컨트랙트에서 미노출되어 현재는 의도적 선택
- 주석(465행): `// 거래 정보 카드 (txHash는 useGetTrade에서 미노출 — 거래 ID로 대체)`

### 수정 작업

#### 작업 6-1: useEscrow에서 release/refund 이벤트의 txHash 수집
```
파일: src/hooks/useEscrow.js
```
- `release()` / `refund()` 트랜잭션의 `tx.hash`를 state에 저장

#### 작업 6-2: TradeRoom 완료 화면에서 tx 링크 우선 표시
```
파일: src/components/TradeRoom.jsx
```
```js
// txHash가 있으면 tx 링크, 없으면 address 링크 fallback
const explorerUrl = txHash
  ? getExplorerUrl(networkKey, { type: 'tx', value: txHash })
  : getExplorerUrl(networkKey, { type: 'address', value: address })
```

### 예상 소요: 45분

---

## 실행 우선순위 및 전체 일정

```
Phase 1 — 즉시 (P0, 릴리즈 차단)
├── P0-1  테스트 복구            ~45분
├── P0-2  ESLint 설정            ~20분 (설정만, 에러 정리 별도)
└── P0-3  네트워크 에러 Toast     ~30분
                                 ─────
                            소계: ~1.5시간

Phase 2 — 다음 스프린트 (P1)
├── P1-1  코드 스플리팅           ~1시간
├── P1-2  XSS 제거               ~20분
├── P1-3  탐색기 tx 링크          ~45분
└── P0-2  린트 에러 정리          ~1~2시간
                                 ─────
                            소계: ~3~4시간

Phase 3 — 백로그 (P2)
├── P2-1  editorconfig/인코딩
└── P2-2  목업→실데이터 연동
```

---

## 의존 관계

```
P0-1 (테스트) ──→ 이후 모든 수정의 회귀 검증 기반
P0-2 (린트)   ──→ P1 이후 코드 품질 게이트
P0-3 (Toast)  ──→ 독립 (병렬 가능)
P1-1 (스플릿) ──→ P1-3과 독립 (병렬 가능)
P1-2 (XSS)   ──→ 독립 (병렬 가능)
P1-3 (링크)   ──→ useEscrow 수정 선행 필요
```

> **권장**: P0-1을 가장 먼저 수행하여 테스트 인프라를 복구한 뒤, 나머지 P0 작업을 병렬로 진행.
