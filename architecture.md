# 🏗️ MiniSwap — 기술 아키텍처

---

## 전체 구조 개요

```
┌─────────────────────────────────────────────────────────┐
│                     사용자 브라우저                        │
│                                                           │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐ │
│  │   React UI   │   │  Trystero    │   │  IndexedDB   │ │
│  │  (모바일 앱)  │   │ (P2P 오더북) │   │ (로컬 저장소) │ │
│  └──────┬───────┘   └──────┬───────┘   └──────────────┘ │
│         │                  │                              │
│  ┌──────┴───────────────────┴──────────────────────────┐ │
│  │              ethers.js / wagmi                       │ │
│  └──────────────────────┬──────────────────────────────┘ │
│                         │                                 │
│  ┌──────────────────────▼──────────────────────────────┐ │
│  │                MetaMask (지갑)                        │ │
│  └──────────────────────┬──────────────────────────────┘ │
└─────────────────────────┼───────────────────────────────┘
                          │ JSON-RPC
┌─────────────────────────▼───────────────────────────────┐
│                    Arbitrum One                           │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │           MiniSwapEscrow.sol                        │  │
│  │  deposit() / release() / refund() / dispute()       │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  ┌──────────────┐           ┌──────────────────────────┐ │
│  │  USDT Token  │           │   2-of-2 MultisigAdmin   │ │
│  │   (ERC-20)   │           │   (분쟁 중재용)           │ │
│  └──────────────┘           └──────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 기술 스택

| 레이어 | 기술 | 역할 |
|--------|------|------|
| **블록체인** | Arbitrum One | 저렴한 Gas (~$0.15/tx) |
| **스마트 컨트랙트** | Solidity ^0.8.x | 에스크로 로직 |
| **지갑 연동** | MetaMask + ethers.js | 트랜잭션 서명 |
| **P2P 네트워킹** | Trystero (WebRTC) | 오더북 브로드캐스트 |
| **로컬 저장소** | IndexedDB | 오더·거래내역 저장 |
| **프론트엔드** | React (모바일 퍼스트) | UI/UX |
| **서버** | 없음 (Serverless) | — |

---

## 스마트 컨트랙트 설계

### MiniSwapEscrow.sol

```solidity
struct Trade {
    address seller;      // 판매자 주소
    address buyer;       // 구매자 주소
    uint256 amount;      // USDT 금액
    uint256 feeAmount;   // 수수료 (2%)
    uint256 createdAt;   // 생성 시간
    uint256 expiresAt;   // 만료 시간 (7일)
    TradeStatus status;  // LOCKED / RELEASED / DISPUTED / REFUNDED
}

enum TradeStatus { LOCKED, RELEASED, DISPUTED, REFUNDED }

// 핵심 함수
function deposit(address buyer, uint256 amount) external;   // 에스크로 락
function release(bytes32 tradeId) external;                  // KRW 확인 후 릴리즈
function refund(bytes32 tradeId) external;                   // 타임아웃 환불
function dispute(bytes32 tradeId) external;                  // 분쟁 신청
function adminResolve(bytes32 tradeId, bool toSeller) external; // 중재 판정
```

### 에스크로 상태 흐름

```
deposit()
    │
    ▼
 LOCKED ──── release() ──────► RELEASED (정상 완료)
    │
    ├──── dispute() ──────────► DISPUTED
    │                               │
    │                         adminResolve()
    │                               │
    │                    ┌──────────┴──────────┐
    │                    ▼                     ▼
    │                 toSeller=true        toSeller=false
    │                (판매자 승리)          (구매자 승리)
    │
    └──── refund() ───────────► REFUNDED (7일 타임아웃)
```

### Gas 최적화 전략

```
오더 올리기: Gas 없음
  → Trystero P2P 메시지로 브로드캐스트
  → IndexedDB에 로컬 저장

에스크로 락 (deposit): ~$0.10~0.15
  → ERC-20 approve + deposit 2번 트랜잭션
  → approve는 max uint256으로 1회 처리 가능

릴리즈 (release): ~$0.05
  → 판매자가 서명, 1번 트랜잭션
```

---

## P2P 오더북 아키텍처 (Trystero)

### 오더 타입

```typescript
// 판매 오더
type SellOrder = {
  id: string;
  type: 'SELL';
  seller: string;       // 0x 주소
  amount: number;       // USDT 수량
  priceKRW: number;     // KRW/USDT 환율
  bankAccount: string;  // 입금 계좌 (암호화)
  expiry: number;       // Unix timestamp
  signature: string;    // MetaMask 서명
}

// 구매 오더
type BuyOrder = {
  id: string;
  type: 'BUY';
  buyer: string;        // 0x 주소
  amount: number;       // 원하는 USDT 수량
  priceKRW: number;     // KRW/USDT 환율
  expiry: number;
  signature: string;
}
```

### Trystero 채널 구조

```
Room: "miniswap-orderbook-mainnet"
  ├── Channel: "sell-orders"   → 판매 오더 브로드캐스트
  ├── Channel: "buy-orders"    → 구매 오더 브로드캐스트
  ├── Channel: "accept-req"    → 수락 요청 (구매자 → 판매자)
  └── Channel: "accept-res"    → 수락 응답 (판매자 → 구매자)
```

### Race Condition 해결

```
구매자 A, B, C 동시 수락 요청
         │
         ▼
판매자에게 accept-req 수신 (타임스탬프 포함)
         │
         ▼
판매자가 UI에서 1명 선택 (명시적 선택)
         │
  ┌──────┴──────┐
  ▼             ▼
선택된 A     B, C에게 reject 메시지
에스크로 락    (P2P 채널로 자동 전송)
```

---

## 거래 플로우 상세

### 플로우 A: 판매 오더 수락

```
판매자                                구매자
─────                                ─────
S06: 오더 작성
  │
  └── P2P 브로드캐스트 ─────────────► S03: 오더북에 표시
                                      S11: 오더 상세 확인
                                      S11: 수락 요청 전송
  ◄──────────────────────────────────
S07: 수락 요청 수신 (여러 명 가능)
S07: 구매자 1명 선택
  │
S08: MetaMask 팝업 ×2
  ├── approve(escrow, amount)
  └── deposit(buyer, amount)
                                     S12: 에스크로 확인
                                          (Arbitrum TX 검증)
                                     S13: 계좌 확인 후 KRW 송금
S09: KRW 입금 확인
  └── release(tradeId)               S14: USDT 수령 + 평점
S10: 판매 완료 + 평점
```

### 플로우 B: 구매 오더 수락

```
구매자                                판매자
─────                                ─────
S15: 구매 오더 작성
  │
  └── P2P 브로드캐스트 ─────────────► S04: 오더북에 표시
S16: 대기 중                          S17: 구매오더 상세 확인
                                      S18: 수락 클릭
                                        └── MetaMask ×2
                                              approve + deposit
  ◄─ 알림 + 계좌 공개 ─────────────────
S13: KRW 송금
S14: 완료 + 평점                      S09: KRW 확인 → release
                                      S10: 완료 + 평점
```

**플로우 A vs B 비교**

| 항목 | 플로우 A | 플로우 B |
|------|---------|---------|
| 오더 게시자 | 판매자 | 구매자 |
| 에스크로 락 주체 | 판매자 (수락 후) | 판매자 (수락 즉시) |
| 구매자 선택 단계 | 있음 (S07) | 없음 |
| 총 단계 수 | 판매자 5단계 | 판매자 4단계 |
| 특징 | 여러 구매자 중 선택 가능 | 1:1 즉시 매칭 |

---

## 로컬 데이터 저장 (IndexedDB)

```typescript
// DB 스키마
const DB_SCHEMA = {
  orders: {
    keyPath: 'id',
    indexes: ['type', 'seller', 'buyer', 'status', 'expiry']
  },
  trades: {
    keyPath: 'tradeId',
    indexes: ['seller', 'buyer', 'status', 'createdAt']
  },
  settings: {
    keyPath: 'key'
    // 'bankAccount', 'nickname' 등
  }
}
```

**저장 원칙**: 민감 정보(계좌번호)는 로컬만 저장, 서버 전송 없음

---

## MetaMask 연동

### 지갑 연결

```javascript
// 1. 연결
const provider = new ethers.BrowserProvider(window.ethereum);
await provider.send("eth_requestAccounts", []);

// 2. 네트워크 확인
const network = await provider.getNetwork();
if (network.chainId !== 42161n) {
  // Arbitrum chainId = 42161
  await window.ethereum.request({
    method: 'wallet_switchEthereumChain',
    params: [{ chainId: '0xa4b1' }]
  });
}

// 3. 서명자 획득
const signer = await provider.getSigner();
const address = await signer.getAddress(); // 이게 곧 사용자 신원
```

### 에스크로 트랜잭션

```javascript
// approve
const usdt = new ethers.Contract(USDT_ADDRESS, ERC20_ABI, signer);
await usdt.approve(ESCROW_ADDRESS, amount);

// deposit
const escrow = new ethers.Contract(ESCROW_ADDRESS, ESCROW_ABI, signer);
await escrow.deposit(buyerAddress, amount);

// release (판매자가 KRW 확인 후 서명)
await escrow.release(tradeId);
```

---

## 오더 서명 검증

P2P로 전달된 오더의 위변조 방지:

```javascript
// 오더 서명 (오더 작성 시)
const messageHash = ethers.solidityPackedKeccak256(
  ['string', 'uint256', 'uint256', 'uint256'],
  [orderId, amount, priceKRW, expiry]
);
const signature = await signer.signMessage(ethers.getBytes(messageHash));

// 서명 검증 (오더 수신 시)
const recovered = ethers.verifyMessage(ethers.getBytes(messageHash), signature);
if (recovered.toLowerCase() !== order.seller.toLowerCase()) {
  // 위조된 오더 → 무시
}
```

---

## 평점 시스템

```
거래 완료 후 양방향 평점
  ├── 판매자 → 구매자 평점 (1~5점)
  └── 구매자 → 판매자 평점 (1~5점)

저장 위치: IndexedDB (로컬)
집계 방식: 수신한 모든 평점의 평균
표시 형태: ★ 4.9 · 거래 127회

패널티:
  └── 10분 내 에스크로 락 미완료 시 → 자동 -1점
```

---

## 분쟁 해결

```
분쟁 신청 (S19)
  │
  ├── 자동 수집 증거
  │     ├── 오더 데이터 (판매자 서명 포함)
  │     ├── P2P 채팅 내역
  │     └── Arbitrum 에스크로 TX
  │
  ├── 사용자 제출 증거
  │     └── 송금 확인증 캡처 (선택)
  │
  └── 운영자 2명 검토 (24~48시간)
        │
        ├── 판매자 승리 → release(toSeller=true)
        └── 구매자 승리 → release(toSeller=false)
```

---

## 보안 고려사항

| 위협 | 대응 |
|------|------|
| 오더 위변조 | MetaMask 서명 검증 |
| Race Condition | 판매자 명시적 선택 |
| 릴리즈 강제 | 에스크로 컨트랙트 (판매자만 호출 가능) |
| 잘못된 금액 입력 | 복사 버튼 + 강제 체크박스 |
| 잘못된 계좌 입력 | 복사 버튼 + 확인 체크박스 |
| 네트워크 오류 | 자동 체인 전환 (wallet_switchEthereumChain) |
| 타임아웃 미처리 | 7일 후 자동 refund() |

---

## 파일 구조 (예상)

```
miniswap/
├── contracts/
│   ├── MiniSwapEscrow.sol
│   └── test/
│       └── Escrow.test.js
├── src/
│   ├── components/
│   │   ├── OrderBook/
│   │   ├── TradeFlow/
│   │   └── Profile/
│   ├── hooks/
│   │   ├── useMetaMask.js
│   │   ├── useEscrow.js
│   │   └── useOrderBook.js
│   ├── lib/
│   │   ├── trystero.js      ← P2P 오더북
│   │   ├── indexeddb.js     ← 로컬 저장
│   │   └── signature.js     ← 서명·검증
│   └── App.jsx
├── miniswap-v4.html         ← 인터랙티브 프로토타입
├── README.md
└── architecture.md
```
