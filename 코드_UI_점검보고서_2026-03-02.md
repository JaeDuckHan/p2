# MiniSwap 코드/UI 점검 보고서 (2026-03-02)

## 1) 점검 범위
- 기준 문서: `worklog.md`
- 점검 대상: `src/`, `contracts/`, `test/`, 빌드/린트/테스트 스크립트
- 실행 검증:
  - `npm run build` ✅ 통과
  - `npm run lint` ❌ 실패
  - `npm test` ❌ 실패 (102건)

---

## 2) 핵심 결론
- 프론트엔드 UI는 빌드 기준으로는 동작 가능 상태다.
- 다만 품질/유지보수 관점에서 즉시 보완이 필요한 항목이 3개(P0) 있다.
- 특히 컨트랙트 테스트 전면 실패는 릴리즈 차단 이슈다.

---

## 3) 우선순위별 이슈와 수정안

## P0 (즉시 수정)

### P0-1. 컨트랙트 테스트 102건 전면 실패
- 현상:
  - `npm test` 결과 `0 passing`, `102 failing`
  - 공통 에러: `incorrect number of arguments to constructor`
- 원인:
  - 컨트랙트 생성자 인자 5개(`_relayer` 추가)인데 테스트는 4개 인자로 배포 중
- 근거 파일:
  - `contracts/MiniSwapEscrow.sol:252` (5개 인자 생성자)
  - `test/MiniSwapEscrow.test.js:101` (4개 인자 배포)
- 수정안:
  1. 테스트 배포부 전체에 `relayer` 인자 추가
  2. `ZeroAddress` 검증 테스트에 `_relayer` 케이스 추가
  3. 회귀 확인: `npm test` 재실행

### P0-2. 린트 스크립트가 현재 실행 불가
- 현상:
  - `npm run lint` 시 ESLint v9 설정 파일 미존재 오류
- 원인:
  - `package.json`은 ESLint 9인데 `eslint.config.js`(flat config) 없음
- 근거 파일:
  - `package.json` (`"eslint": "^9.0.0"`, `"lint": "eslint src --ext .js,.jsx"`)
- 수정안:
  1. `eslint.config.js` 생성(React JSX 기준 최소 규칙)
  2. 팀이 기존 `.eslintrc` 스타일을 원하면 ESLint를 v8로 내리는 선택지도 가능
  3. CI에 `npm run lint` 추가

### P0-3. 네트워크 전환 실패 시 사용자 피드백 누락
- 현상:
  - 네트워크 자동 전환 실패 메시지는 훅 내부 state에만 저장되고 UI에 노출되지 않음
- 원인:
  - `useNetworkSwitch()`의 `error`를 `App/AppShell`에서 사용하지 않음
- 근거 파일:
  - `src/hooks/useNetworkSwitch.js` (`error` state 관리)
  - `src/App.jsx` (`const { switchNetwork, switching } ...`만 사용)
- 수정안:
  1. `App`에서 `error`를 받아 `ToastContext` 또는 배너로 표출
  2. 에러 메시지 발생 시 재시도 버튼/가이드 링크 제공(`chainlist.org`)

---

## P1 (다음 스프린트 권장)

### P1-1. 번들 크기 과대 (초기 로딩 부담)
- 현상:
  - 빌드 경고: 메인 청크 약 1MB+, wasm 약 11MB
- 영향:
  - 모바일/저사양 환경에서 첫 로딩 지연 가능
- 수정안:
  1. `TradeRoom`, XMTP 관련 모듈 dynamic import 분리
  2. 라우트/뷰 단위 코드 스플리팅
  3. 필요 시 `manualChunks` 도입

### P1-2. `dangerouslySetInnerHTML` 사용 최소화 필요
- 현상:
  - Stepper에서 HTML 라벨 렌더링
- 위험:
  - 현재는 내부 상수 기반이라 즉시 취약점은 아니지만, 추후 외부 데이터 유입 시 XSS 위험
- 근거 파일:
  - `src/components/ui/stepper.jsx:47`
- 수정안:
  1. HTML 문자열 대신 plain text + 줄바꿈 처리로 대체
  2. 반드시 HTML이 필요하면 sanitize 유틸 적용

### P1-3. 거래 완료 화면 탐색기 링크 개선
- 현상:
  - 완료 화면 링크가 트랜잭션이 아닌 사용자 address로 이동
- 근거 파일:
  - `src/components/TradeRoom.jsx:492`
- 수정안:
  1. 가능하면 `release/refund` tx hash 저장 후 tx 링크 제공
  2. 보조로 컨트랙트 주소 링크 병행

---

## P2 (개선 제안)

### P2-1. `worklog.md` 인코딩/표준화
- 현상:
  - 에디터/터미널 환경에 따라 한글 깨짐 가능성
- 수정안:
  1. 저장 인코딩 UTF-8 고정
  2. `.editorconfig`에 `charset = utf-8` 명시
  3. 문서 제목/섹션 규칙 템플릿화

### P2-2. UI 신뢰 요소(Trust Signal) 고도화
- 현상:
  - 평점/거래 수가 목업(`src/mockData.js`) 기반
- 수정안:
  1. 실제 온체인 집계 연동 전까지 “베타/예시 데이터” 라벨 표시
  2. 이후 서브그래프 또는 인덱서 연동으로 실데이터 치환

---

## 4) worklog 기준 반영 상태 요약
- 반영 완료로 확인:
  - 네트워크 분리(EVM/Tron), WalletButton 모달, OnboardBanner, 3탭 네비, 거래내역 페이지, 토스트 시스템
- 미완/후속 과제로 확인:
  - Tron escrow 실제 구현(`useTronEscrow`는 현재 no-op)
  - 성능 최적화(코드 스플리팅)
  - 배포/검증 자동화 및 린트 체계

---

## 5) 권장 실행 순서
1. P0-1 테스트 복구(생성자 인자 정합)
2. P0-2 ESLint 설정 정상화
3. P0-3 네트워크 전환 실패 UI 피드백 추가
4. P1-1 번들 최적화(코드 스플리팅)

